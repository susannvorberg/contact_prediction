# Contact Prediction Framework
collection of frequently used scripts

#Installing
Clone this repository using git clone --recursive. 
Then, compile C++ extensions with command:

 python setup.py build_ext --inplace
